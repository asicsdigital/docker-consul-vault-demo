#!/usr/bin/env bash

echo $USER
#cd /usr/share/nginx/html && sed -e s/Docker/"$AUTHOR"/ Hello_docker.html > index.html ; nginx -g 'daemon off;'
#cd /usr/share/nginx/html && sed -e s/Docker/"$USER"/ Hello_docker.html > index.html ; nginx -g 'daemon off;'


#cd /usr/share/nginx/html
#sed -e s/Docker/"$USER"/ Hello_docker.html > index.html
#envconsul -log-level=debug -config=config.hcl nginx -g 'daemon off;'
